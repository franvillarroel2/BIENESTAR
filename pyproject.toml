[project]
name = "bienestar-api"
version = "0.1.0"
description = "API para el proyecto Bienestar"
requires-python = ">=3.9"
dependencies = [
    "fastapi",
    "uvicorn"
]

[tool.vercel]
entrypoint = "app:app"